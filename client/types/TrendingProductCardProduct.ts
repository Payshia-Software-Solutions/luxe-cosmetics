// Assuming we're using the Product interface from your previous code
export interface Product {
    id: string;
    slug: string;
    name: string;
    price: number;
    rating: number;
    review: number;
    description: string;
    images: string[];
    category: string;
    // other properties...
}
